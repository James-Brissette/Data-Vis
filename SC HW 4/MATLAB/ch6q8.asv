function [error] = ch6q8b(n)
% Solves for h using the trapezoidal rule
a = 0;
b = 2;

trueError = .995322265;

x = linspace(a,b,n+1);
h = x(2) - x(1);

IT = y(x);
IT(1) = .5*IT(1);
IT(n+1) = .5*IT(n+1);
IT = IT.*h;

error = a

end

function y=f(x)
   y = 2/sqrt(pi).*exp(-x.*x);
end


