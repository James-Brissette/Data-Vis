function [output] = ch6q8(n)
% Solves for h using the trapezoidal rule
a = 0;
b = 2;
x = linspace(a,b,n+1);

IT = 0;
trueError = 0.995322265;

IT = .5*f(x(1));
for i=2:n
    IT = IT + f(x(i));
end
IT = IT + .5*f(x(1));

error = abs


end

function y=f(x)
    y = 2\sqrt(pi)*exp(-x*x);
end

